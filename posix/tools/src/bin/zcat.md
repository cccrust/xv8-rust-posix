# Zcat — 檢視壓縮檔案

`zcat` 讀取由 `compress` 壓縮的 `.Z` 檔案，解壓縮後輸出到 stdout，不寫入磁碟。等同於 `uncompress -c`。此工具允許使用者檢視壓縮檔案內容而無需明確解壓。Zcat 是 `gzip -dc` 與 `uncompress -c` 的概念先驅。

## 相關文件

- [compress.md](./compress.md) — 壓縮
- [uncompress.md](./uncompress.md) — 解壓縮
