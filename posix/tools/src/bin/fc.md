# Fc — 歷史命令管理

`fc`（fix command）管理 shell 的歷史命令列表。`fc -l` 列出歷史命令，`fc -s` 重新執行歷史命令。`fc` 也允許使用者將歷史命令匯出到編輯器，修改後再執行。這是互動式 shell 生產力的關鍵功能。

## 相關文件

- [sh.md](./sh.md) — Shell
- [history.md](./history.md) — 歷史命令
