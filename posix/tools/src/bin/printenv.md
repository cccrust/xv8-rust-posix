# Printenv — 印出環境變數

`printenv` 列出所有環境變數與其值，或顯示指定變數的值。環境變數是 shell 與子行程共享的鍵值對，包含 PATH、HOME、USER、SHELL、TERM 等。不同於 `env`，`printenv` 僅輸出而不修改環境。`set` 命令在 shell 中也執行類似功能。

## 相關文件

- [env.md](./env.md) — 修改環境並執行程式
- [sh.md](./sh.md) — Shell 環境變數
