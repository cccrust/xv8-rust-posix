# Alias — 建立命令別名

`alias` 命令用於在 shell 中建立命令別名，讓使用者以短名稱執行長命令。別名儲存在 shell 的內部表中，通常用於簡化常用命令（如 `alias ll='ls -l'`）。`unalias` 反向操作。別名展開是 shell 展開順序中的第一步——在變數展開之前。

## 相關文件

- [unalias.md](./unalias.md) — 移除別名
- [sh.md](./sh.md) — Shell 命令直譯器
