# Printf — 格式化輸出

`printf` 根據格式字串格式化並輸出資料。它源自 C 語言的 `printf()` 函式，支援格式指定符（`%s` 字串、`%d` 整數、`%f` 浮點數、`%x` 十六進位、`%%` 跳脫）。不同於 `echo`，`printf` 提供精確的輸出控制，在腳本中是跨平台的標準輸出方法。

## 相關文件

- [echo.md](./echo.md) — 簡單輸出
- [fmt.md](./fmt.md) — 文字格式化
