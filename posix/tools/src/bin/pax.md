# Pax — 可攜式歸檔交換工具

`pax`（portable archive exchange）是 POSIX 標準的歸檔工具，統一 tar 與 cpio 的介面。`pax` 支援多種歸檔格式（ustar、cpio、pax），作為 tar、cpio 與 dump 的通用替代品。`pax -w`（寫入模式）建立歸檔，`pax -r`（讀取模式）提取，`pax -rw`（複製模式）拷貝目錄樹。

## 相關文件

- [tar.md](./tar.md) — Tape archive
- [cpio.md](./cpio.md) — Copy in/out
