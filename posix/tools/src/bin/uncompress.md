# Uncompress — 解壓縮 .Z 檔案

`uncompress` 恢復由 `compress` 壓縮的 `.Z` 檔案。使用 LZW 解壓縮演算法將壓縮檔案還原為原始內容。`.Z` 格式在早期 Unix 廣泛使用，後被 `gzip`（`.gz`）與 `bzip2`（`.bz2`）取代。

## 相關文件

- [compress.md](./compress.md) — 壓縮檔案
- [zcat.md](./zcat.md) — 檢視壓縮檔案（不解壓縮）
