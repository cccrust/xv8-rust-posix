# Curl — HTTP 傳輸工具

`curl` 是一個命令列 HTTP 客戶端工具，用於發送 HTTP 請求和接收回應。它支援 GET、POST、PUT、DELETE 等方法，可自訂標頭、處理 cookie、跟隨重定向（Location）。`curl` 在自動化腳本與 API 測試中廣泛使用，支援 HTTP/1.1 的請求和回應解析。

## 相關文件

- [httpget.md](./httpget.md) — HTTP GET 工具
- [http_server.md](./http_server.md) — HTTP 伺服器
