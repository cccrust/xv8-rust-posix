# Host — 主機名稱查詢

`host` 是一個精簡的 DNS 查詢工具，專注於將主機名稱解析為 IP 位址。它類似 `nslookup` 的簡化版本，執行單一 DNS A 記錄查詢並顯示結果。此工具對於快速檢查域名指向的 IP 位址非常實用，無需完整安裝 `dig` 或 `nslookup`。

## 相關文件

- [dns.md](./dns.md) — DNS 查詢工具
- [dns.md](../../libnet/src/proto/dns.md) — DNS 協定
