# 事件模組總覽

提供跨平台的非同步事件處理架構，支援鍵盤、滑鼠與終端尺寸變更事件。
核心設計圍繞事件來源（`EventSource`）、事件串流（`EventStream`）與
輪詢（poll）機制。支援兩種後端：**mio**（非同步 I/O 多路複用）與
**TTY**（基於檔案描述子的阻塞讀取）。事件讀取支援 Blocking 與 Non-blocking
模式，並可設定讀取逾時。此模組是互動式終端應用程式的基礎。
