# Mio 事件來源

基於 mio（Metal I/O）非同步 I/O 多路複用程式庫的事件來源實作。
mio 提供跨平台的 `Poll` 與 `Events` 抽象，可在單一執行緒中監控
多個檔案描述子的可讀/可寫狀態。此實作將終端的 stdin 檔案描述子
註冊到 mio 的 Poll 實例，並在事件發生時觸發回呼。
mio 後端支援更高的效能與可擴展性，適合需要在事件迴圈中混合
網路 I/O 與終端 I/O 的應用程式。
