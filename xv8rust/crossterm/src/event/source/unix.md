# Unix 事件來源分派

在 Unix 平台上根據啟用的功能特徵選擇後端：啟用 `mio` 特徵時使用
mio 事件來源（高效能非同步 I/O），否則使用 TTY 事件來源（基於檔案描述子）。
此模組處理終端初始設定（啟用滑鼠追蹤、切換到替代緩衝區等），
並提供中斷處理（處理 SIGWINCH 等訊號）。Unix 事件來源的設計
權衡了效能（mio 的事件驅動模型）與相容性（TTY 的阻塞讀取模型）。
