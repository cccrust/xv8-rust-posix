# 游標移動模組

管理終端游標的位置、可見性與形狀。提供的操作包括：移動游標到絕對/相對座標、
儲存/恢復游標位置、顯示/隱藏游標、啟用/停用游標閃爍、查詢游標位置。
所有操作皆透過 ANSI 逸出碼（如 `CPR`、`CUP`、`DECTCEM`）或
Windows Console API 實作。此模組是終端 UI 互動的基礎設施，
支援 `TerminalCursor` 型別作為主要操作入口。
