# crossterm 程式庫概覽

crossterm 是一個跨平台終端控制程式庫，提供游標控制、樣式設定、終端操作
（原始模式、尺寸查詢）與事件處理（鍵盤、滑鼠、尺寸變更）等功能。
此版本為 0.29.0 的移植，針對 xv8 RISC-V 目標加入 `riscv64` 平台支援，
並透過 `cfg` 條件編譯在各平台間切換實作。核心設計圍繞 `Command` trait，
所有操作皆可排入佇列後批次執行，減少系統呼叫次數。
