# v1.4 — ls -R 與 diff LCS

**目標：補強兩個常用工具的功能完整性。**

---

## 結果：已完成

檢查後發現 `ls -R` 和 `diff` LCS 在現有程式碼中**早已實作完成**：

### ls -R

| 位置 | 說明 |
|------|------|
| `ls.rs:143` | `-R` flag 解析：`'R' => cfg.recursive = true` |
| `ls.rs:244-253` | 遞迴遍歷：走訪所有子目錄（跳過 `.`、`..`）並遞迴呼叫 `list_dir()` |

### diff LCS

| 位置 | 說明 |
|------|------|
| `diff.rs:139-177` | 經典 DP LCS 演算法：O(mn) DP table → backtrace 產生 DiffOp（Same/Delete/Insert） |
| `diff.rs:46-103` | Unified diff 格式輸出（含 context lines、@@ header） |

---

## 驗收結果

| 測試 | 結果 |
|------|------|
| `test_ls_recursive` | ok |
| `test_diff_identical` | ok |
| `test_diff_different` | ok |
| `test_diff_lcs_insert` | ok |
| `test_diff_lcs_delete` | ok |
